# ── Claude / Anthropic ────────────────────────────────────────────────────────
# Choose ONE authentication method:
#
# Option A — API key: set this variable.
# ANTHROPIC_API_KEY=sk-ant-...
#
# Option B — Account login: leave this commented out and instead uncomment
#   the volumes section in docker-compose.yml. Run `claude login` on your
#   host machine first so ~/.claude.json exists with a valid session.

# ── Git repository ────────────────────────────────────────────────────────────
# SSH URL preferred (requires GIT_SSH_PRIVATE_KEY below)
GIT_REPO_URL=git@github.com:your-username/fitness.git

# Git commit identity
GIT_USER_EMAIL=claude-docker@yourdomain.com
GIT_USER_NAME=Claude Docker

# SSH private key for git authentication (base64-encoded, must be a single line)
# Generate a deploy key: ssh-keygen -t ed25519 -C "claude-docker" -f ~/.ssh/claude_docker
# Add the PUBLIC key as a Deploy Key in GitHub repo settings (with write access).
# Then encode the private key: base64 -i ~/.ssh/claude_docker | tr -d '\n'
GIT_SSH_PRIVATE_KEY=

# ── Project .env variables ────────────────────────────────────────────────────
# Any variable prefixed with PROJECT_ will be written to the repo's .env file
# with the PROJECT_ prefix stripped. Add all your project secrets here.
PROJECT_TELEGRAM_BOT_TOKEN=
PROJECT_TELEGRAM_ALLOWED_USER_ID=
PROJECT_ANTHROPIC_API_KEY=
PROJECT_OPENAI_API_KEY=
PROJECT_DATABASE_URL=sqlite:///./fitness.db
PROJECT_GARMIN_SYNC_HOUR=3
PROJECT_MAX_HR=185

# ── Optional: non-interactive prompt ─────────────────────────────────────────
# If set, Claude runs this prompt and exits instead of starting interactively.
# Useful for CI/CD or scheduled tasks.
# CLAUDE_PROMPT=Run the test suite and fix any failing tests, then commit and push.
