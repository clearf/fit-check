# ── Garmin authentication ───────────────────────────────────────────────────
# DO NOT put your Garmin credentials here for normal use.
# Run `python -m fitness setup` once — it authenticates interactively and
# saves session cookies to ~/.fitness/garmin_session/ (never to .env).
#
# These fields are only used if you want to automate the setup step in CI
# or scripted environments (leave blank otherwise):
GARMIN_EMAIL=
GARMIN_PASSWORD=

# ── Telegram ─────────────────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=1234567890:ABCDEF...
# Your Telegram user ID (get it from @userinfobot) — bot only responds to this user
TELEGRAM_ALLOWED_USER_ID=123456789

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

DATABASE_URL=sqlite:///./fitness.db

# Your max heart rate for HR zone calculations
MAX_HR=185

# Hour (24h) for nightly backup sync
GARMIN_SYNC_HOUR=3
